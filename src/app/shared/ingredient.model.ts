export class Ingredient {
        
    // public name: string;
    // public amount: number;
    // can just add public in front of arguments in constructor
    
    constructor(public name: string, public amount: number) {
        // this.name = name;
        // this.amount = amount;
    }
}